<Window x:Class="techo.MasterCommit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:techo"
        mc:Ignorable="d"
        Title="MasterComments" Height="450" Width="800">
   
        <Grid Margin="30" VerticalAlignment="Center" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
           
            <Label Grid.Row="1" Content="Выберите номер заявки:" />
        <ComboBox Grid.Row="2" x:Name="RequestComboBox" DisplayMemberPath="RequestID" 
                  SelectedValuePath="RequestID" Margin="10" SelectionChanged="RequestComboBox_SelectionChanged">  </ComboBox>
            <Label Grid.Row="3"  Content="Комментарий:" />
            <TextBox Grid.Row="4" Height="40" x:Name="CommentTextBox" />
            <Button Grid.Row="5"  Content="Добавить" Click="Button_AddComment_Click"/>
        </Grid>
    
</Window>
